/**
 * Compatibility bootstrap path for older app shells.
 *
 * This repository is open-source-only. New entrypoints should import
 * `./bootstrap/oss`; this path remains for external callers that still import
 * `./bootstrap/integrated` for side effects.
 */

import "./oss";

export {};
